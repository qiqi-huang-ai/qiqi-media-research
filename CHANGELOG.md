# Changelog

## 0.2.0 — 2026-09-20

- 短提示词会按研究模式自动补全成熟交付合同；新增单账号审计协议、高低表现对照、文本模式统计、代表作品评论和可执行建议。
- 用户报告不再显示内部 evidence ID 或证据类别；内部证据改存 `analysis/findings.json`，并纳入交付验收。
- 账号审计默认检查账号资料、表现基线、内容模式、分组对照和行动建议，缺项时不得包装为成熟报告。
- 声明 PDF 渲染所需的 ReportLab 运行依赖，并统一 Markdown/PDF 的证据展示边界。
- 运行器为每次执行写入不含凭证的 `manifest.json`，并记录实际原始响应文件。
- 修正评论挖掘调用计划；跨平台 CLI 现在会创建两个平台适配器。
- 明确拒绝小红书趋势扫描，并支持 CLI 的 `--sample-pages 1..3`。
- 保留抖音 Search API 的下一页状态，避免多页检索丢失 `search_id` 后失败。
- 爆款与评论研究补充抖音独立统计端点，读取真实播放量并在 PDF 中隐藏内部证据标记。
- 账号审计改为两阶段决策报告：完整原始明细留在证据包，读者报告必须回答定位、规律与反例、策略、公开需求、借鉴边界和验证计划。
- 可视化 PDF 改为 Markdown 正文的阅读版；账号图表只读取同次数据包，不把不可见的曝光、完播、转化数据伪装成事实。
- 新增两种安装入口与 `scripts.bootstrap`：代理可自动创建隔离环境、安装运行依赖与运行诊断；密钥始终由用户在安全环境变量中自行配置。

## 0.1.0 — 2026-09-18

首个可开源版本。

### 已完成

- 11 种自媒体研究模式的 Skill 路由和研究规范。
- TikHub REST-only 架构，Key 只从本机环境读取，不依赖连接器模式。
- 抖音 App V3 与 Search API 适配。
- 小红书 App V2 适配。
- 原始证据、标准化 JSONL、确定性指标、机会评分和证据化 Markdown 报告。
- 调用次数预估、超过 20 次的扩量提醒和无付费 `doctor` 诊断。
- Codex、Claude Code、Cursor、WorkBuddy 入口桥接。
- 脱敏夹具端到端测试和有限真实验收。

### 真实验收边界

- 抖音：搜索、账号资料、账号作品、作品详情、评论链路通过低量真实验收。
- 小红书：搜索、账号资料、笔记详情、评论链路通过低量真实验收。
- 小红书账号作品接口在验收中返回 HTTP 400，保持 `available-unverified`，不宣传为已通过。
- 评论回复、抖音热榜等未纳入本次真实验收。

### 已知限制

- TikHub 价格、额度、返回结构和接口状态可能变化；使用者需要自行承担费用并在本地验收。
- 当前版本以抖音和小红书为首版平台，其他平台不属于已验证范围。
- 11 种模式已完成统一路由和底层能力，专用研究执行器将在后续版本逐步补齐。
